




//1)fixed Будет ли достаточно искать треки только по Id и названию? Или нужно по исполнителям, по альбомам, по продолжительности, по жанру и.тд
//2)fixed Нужно ли делать возможность отдельно добавлять исполнителей и альбомы?
//3)fixed Нужно ли искать отдельно альбомы, исполнителей?
//4)fixed Халтурное "удаление" (сами треки не удаляются, просто в TrackStorage поле sorage пересоздается)
//5)fixed Нужно было использовать Observer?
//Нужно снабдить весь код исключениями
//1)fixed Сделать гуишку
//2)fixed Сделать норальный поиск(по жанрам хотяб) Cjplfnm rkfcc Search и там обрабатывать шаблончики.
//3)fixed Сделать многопоточным
//4)fixed Убрать лишние классы Album, Artist
//5)Запилить нормальное сохранение (корректную сериализацию и десериализацию)
//6)Создать генератор Id для треков и жанров
//7)fixed Связать треки и жанры
//-)fixed нужно сделать начальное состояние, чтобы в таблицу сразу рисовались треки
//8)нужно добавить текущее имя файла. (полезно будет при сохранении, 
//чтобы пользователь не запоминал всякий раз, какой файл редактирует)
//8a) можно вообще выводить название файла на форму.
//9)нужно треки и жанры сохранять везде вместе.
//10)будут косяки с сохранением и открытием файла.
//Казалось бы, на вьюшке мы получаем относительный адрес и передаем
//его в метод TrackStorage.load(str) где str относительный путь.
//если во вьюшке будем сохранять не в каталог проекта, то он всеравно сохранится в каталог проекта.
//если мы будем загружать не из каталога проекта, то прога будет пытаться загрузить из каталога проекта
//СУТЬ: загружать и сохранять через абсолютные адреса. (возможно будут ошибки при существующих папках на русском)
//Ошибка при удалении: либо неправильно генериться Id 
//(есть два трека: tr1 с id=0 и tr2 id=1.   tr1 удаляем, затем добавляем tr3. в итоге на выходе получаем только tr3 с id=1.)
//либо неправильно выбираем номер 
//Необходимо исправить добавление из файла т.к добавляются только треки
//исправить отключение сервака по слову quit. он не вырубается.
//при изменении жанра в треке:
//1-жанр в списке жанров не должен изменяться, если новый жанр не существут, то добавляется новый
//2-из списка треков старого жанра удаляется этот трек и добавляется в список нового жанра.
//при удалении



//надо добавить треков, для проверки поиска по жанрам, изменение жанра при обновлении трека, добавление нового жанра, отсутствие эксепшенов при удалении трека, затеем поиска по этому жанру удаленого трека



//если трек для загрузки не найден, то создается новый FileManeger... необходимо клиенту выводить сообщение, что файл не найден!
//getAllGenre - не приавильно реализован через entry в классе GenreStorage и используется в других местах.





//    protected Object readResolve() {
////        GenreStorage.setStorage(this.storage);
//        return ourInstance;//!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
//    }
//
//    private  class SingletonHelper {
//        private  final GenreStorage INSTANCE = new GenreStorage();
//    }

//сделал поиск по треку, потом по жанру с тем же запросом(ввел id, а не жанр), потом добавил трек и вылезла ошибка.